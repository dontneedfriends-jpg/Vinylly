{
  "name": "@vinylly/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NO_STRIP=1 tauri dev",
    "build": "NO_STRIP=1 tauri build",
    "lint": "cd src-tauri && cargo fmt --check && cargo clippy -- -D warnings",
    "typecheck": "tsc --noEmit",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
